cloud:
  aws:
    credentials:
      accessKey: ${AWS_ACCESS_KEY}
      secretKey: ${AWS_SECRET_KEY}
    stack:
      auto: false
    s3:
      bucket: ${AWS_BUCKET}
    region:
      static: ${AWS_REGION}